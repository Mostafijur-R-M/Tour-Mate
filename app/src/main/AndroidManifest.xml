<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tourmate.mostafijur.tourmate">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SpashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".NavEventListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CreateNewEventActivity" />
        <activity android:name=".DeviceLocationActivity" />
        <activity android:name=".TakePhotoActivity" />
        <activity android:name=".WeatherActivity" />
        <activity
            android:name=".show_Event"
            android:label="Event Details" />
        <activity android:name=".ExpenseActivity" />
        <activity
            android:name=".ExpenseListActivity"
            android:label="Expense List" />
        <activity android:name=".AddBudgetActivity" />
        <activity android:name=".weather_new.weatherMainActivity" />
        <activity android:name=".weather_new.activities.MainWeatherActivity"></activity>
    </application>

</manifest>